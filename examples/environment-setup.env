# Xero API Configuration
# Get these values from your Xero Developer App: https://developer.xero.com/

# Required: Your Xero app's client ID
XERO_CLIENT_ID=your_xero_client_id_here

# Required: Your Xero app's client secret
XERO_CLIENT_SECRET=your_xero_client_secret_here

# Required: Comma-separated list of redirect URIs (must match your Xero app settings)
XERO_REDIRECT_URIS=http://localhost:3000/xero/callback,https://yourapp.com/xero/callback

# Optional: Xero API scopes (space-separated)
# Default scopes are provided but you can customize based on your needs
XERO_SCOPES=openid profile email accounting.settings accounting.reports.read accounting.journals.read accounting.contacts accounting.attachments accounting.transactions offline_access

# Optional: HTTP timeout for Xero API calls in milliseconds (default: 30000)
XERO_HTTP_TIMEOUT=30000

# Optional: Clock tolerance in seconds for token validation (default: 5)
XERO_CLOCK_TOLERANCE=5

# Redis Configuration (for token storage)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_if_any
